module PosicionesElem where 
    -- Implementar una función polimórfica en Haskell que dada una dupla compuesta por un elemento y 
    -- una lista de elementos l devuelva una lista con las posiciones que ocupa dicho elemento en la lista l. 
    -- Debes utilizar la función de orden superior foldl. 

    posElem :: (a,[a]) -> [Int]
    posElem (a,lst) = foldl(\f a acc -> ) 

    --DUDA 